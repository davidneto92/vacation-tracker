<div class="row">
  <h1>All National Parks</h1>

  <ul>
    <% @parks.each do |park| %>
      <li><%= link_to "#{park.full_name}", park_path(park) %>, <%= park.state_abbreviation %></li>
    <% end %>
  </ul>
</div>
